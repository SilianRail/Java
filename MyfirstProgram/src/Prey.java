public interface Prey{
    //this will work like an abstract method
    void flee();

    /* Note that abstract classes can have implimentations
     * while the interface can't have implimentations
     * it can be called total abstraction
     */
}